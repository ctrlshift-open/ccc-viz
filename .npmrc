# pnpm config
# Note: only-built-dependencies causes pnpm action-setup to fail in CI
# Native modules like better-sqlite3 must be built manually if needed:
# bash -c 'cd node_modules/.pnpm/better-sqlite3@*/node_modules/better-sqlite3 && npm run build-release'
