lets add a detail slide over panel on the right of the kanban board.

click a card should select it and display it's details

currently the card is too dense with abbreviated title, and session names, so i'd like to leave it like it is but add a detail view that have vertical scropping and text wrap.

make a plan and reference .planning/codebase/


