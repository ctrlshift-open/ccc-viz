---
phase: 01-panel-shell-card-trigger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/StoryDetailPanel.tsx
  - app/components/StoryCard.tsx
  - app/components/KanbanColumn.tsx
  - app/components/KanbanBoard.tsx
autonomous: true

must_haves:
  truths:
    - "User can open panel by clicking expand button on story card"
    - "User can close panel by clicking X button"
    - "User can close panel by clicking overlay behind panel"
    - "User can close panel by pressing Escape key"
    - "Panel scrolls internally when content exceeds viewport"
  artifacts:
    - path: "app/components/StoryDetailPanel.tsx"
      provides: "Slide-in panel shell with close behaviors"
      min_lines: 50
    - path: "app/components/StoryCard.tsx"
      provides: "Expand button for opening detail panel"
      contains: "onOpenDetail"
    - path: "app/components/KanbanBoard.tsx"
      provides: "Panel state management"
      contains: "selectedStory"
  key_links:
    - from: "app/components/StoryCard.tsx"
      to: "app/components/KanbanBoard.tsx"
      via: "onOpenDetail callback"
      pattern: "onOpenDetail.*story"
    - from: "app/components/StoryDetailPanel.tsx"
      to: "app/components/KanbanBoard.tsx"
      via: "onClose callback"
      pattern: "onClose\\(\\)"
---

<objective>
Create panel shell component with slide animation and wire open/close from story cards

Purpose: Enable users to open a detail panel from any story card, with standard flyout behaviors (X close, overlay close, escape key, scrollable content)
Output: Working panel that opens from card button and closes via X/overlay/escape
</objective>

<execution_context>
@/Users/bryanarendt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanarendt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-panel-shell-card-trigger/01-RESEARCH.md

Reference patterns:
@app/welcome/FileViewer.tsx - escape key, body scroll lock, overlay click patterns
@app/components/StoryCard.tsx - stopPropagation pattern for interactive elements on draggable
@app/components/KanbanBoard.tsx - state management location
@app/components/KanbanColumn.tsx - props threading to StoryCard
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StoryDetailPanel component</name>
  <files>app/components/StoryDetailPanel.tsx</files>
  <action>
Create new StoryDetailPanel component with these behaviors:

Props:
- story: KanbanStory (the selected story to display)
- onClose: () => void (callback to close panel)

Structure:
1. Fixed container (z-50) covering full viewport
2. Semi-transparent overlay (bg-gray-900/80) that calls onClose on click
3. Panel slides in from right: fixed inset-y-0 right-0, w-full max-w-md
4. Slide animation: transform transition-transform duration-300 ease-out
5. Panel content area: overflow-y-auto for scrolling, bg-gray-900 border-l border-gray-700

Features to implement:
- Escape key listener (copy pattern from FileViewer.tsx lines 20-29)
- Body scroll lock (copy pattern from FileViewer.tsx lines 32-38)
- X close button in header (top-right, p-2, text-gray-400 hover:text-white)
- Overlay click-to-close (onClick on overlay div, not panel)
- Placeholder content showing story.title for now (Phase 2 adds real content)
- aria-modal="true", role="dialog" for accessibility

Animation approach:
- Use useState for isVisible, initialized to false
- useEffect on mount: setIsVisible(true) after 10ms delay to trigger entrance
- Panel className uses isVisible ? 'translate-x-0' : 'translate-x-full'

Import Expand or ChevronRight icon from lucide-react for consistency.
  </action>
  <verify>
File exists at app/components/StoryDetailPanel.tsx
TypeScript compiles: pnpm typecheck
Component exports StoryDetailPanel function
  </verify>
  <done>
StoryDetailPanel.tsx exists with escape key, body scroll lock, overlay close, X button, slide animation
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire panel to KanbanBoard and StoryCard</name>
  <files>app/components/KanbanBoard.tsx, app/components/KanbanColumn.tsx, app/components/StoryCard.tsx</files>
  <action>
1. KanbanBoard.tsx:
   - Add state: const [selectedStory, setSelectedStory] = useState<KanbanStory | null>(null)
   - Add handler: const handleOpenDetail = (story: KanbanStory) => setSelectedStory(story)
   - Add handler: const handleCloseDetail = () => setSelectedStory(null)
   - Pass onOpenDetail prop through KanbanColumn to StoryCard
   - Render StoryDetailPanel when selectedStory is not null:
     {selectedStory && <StoryDetailPanel story={selectedStory} onClose={handleCloseDetail} />}
   - Import StoryDetailPanel at top

2. KanbanColumn.tsx:
   - Add onOpenDetail prop: onOpenDetail?: (story: KanbanStory) => void
   - Pass through to StoryCard: onOpenDetail={onOpenDetail}

3. StoryCard.tsx:
   - Add onOpenDetail prop: onOpenDetail?: () => void
   - Add expand button in header row (next to menu button):
     <button
       onClick={(e) => { e.preventDefault(); e.stopPropagation(); onOpenDetail?.(); }}
       className="p-1 text-gray-400 hover:text-blue-400"
       title="View details"
     >
       <Expand className="w-4 h-4" />
     </button>
   - Import Expand from lucide-react
   - Place expand button BEFORE the menu button (left of the ...)
  </action>
  <verify>
pnpm typecheck passes
pnpm dev runs without errors
Open browser to http://localhost:5174/kanban
Click expand button on any story card
Panel slides in from right
Click X - panel closes
Click overlay - panel closes
Press Escape - panel closes
  </verify>
  <done>
Expand button appears on story cards
Clicking it opens panel from right
Panel closes via X, overlay click, or Escape key
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. pnpm typecheck - no TypeScript errors
2. pnpm dev - dev server starts
3. Navigate to /kanban in browser
4. Verify story cards show expand button (left of ... menu)
5. Click expand button - panel slides in from right
6. Verify panel shows story title as placeholder content
7. Click X button - panel slides out and closes
8. Open panel again, click dark overlay - panel closes
9. Open panel again, press Escape - panel closes
10. Open panel with long content - verify internal scroll works (panel content scrolls, not page)
</verification>

<success_criteria>
- StoryDetailPanel.tsx created with all close behaviors
- Expand button appears on every story card
- Panel opens without interfering with card drag-and-drop
- Panel closes via X button, overlay click, or Escape key
- Body scroll is locked while panel is open
- Panel content is scrollable
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-panel-shell-card-trigger/01-01-SUMMARY.md`
</output>
