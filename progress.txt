## Codebase Patterns

- Package manager: pnpm
- Test framework: vitest
- Project type: React Router 7 with SSR
- E2E: playwright
- SQLite: better-sqlite3 with drizzle-orm

---

## 2026-01-26 - Phase 01: SQLite Database (Task 6/9)

- Created scripts/migrate-json-to-sqlite.ts - One-time migration script
- Reads kanban.json + kanban-archive.json, writes to SQLite via saveKanbanStateToDb()
- Files changed: scripts/migrate-json-to-sqlite.ts, package.json
- **Learnings:**
  - `npx tsx` runs TypeScript files directly - shebang: `#!/usr/bin/env npx tsx`
  - Verification pattern: compare counts before/after migration
  - Backup strategy: rename JSON files with timestamp suffix before deleting
  - --dry-run flag pattern: `process.argv.slice(2).includes("--dry-run")`
  - Can reuse existing query functions (`saveKanbanStateToDb`) from app code in scripts

---

## 2026-01-26 - Phase 01: SQLite Database (Task 5/9)

- Created app/db/queries.server.ts - Drizzle query helpers
- Functions: getAllStories, getStoryById, createStory, updateStory, archiveStory, etc.
- Files changed: app/db/queries.server.ts
- **Learnings:**
  - Drizzle's `eq`, `and`, `sql` from drizzle-orm for query building
  - NULL comparisons: `sql\`${table.field} IS NULL\`` not `eq(table.field, null)`
  - `onConflictDoUpdate` for upsert: `db.insert(table).values(...).onConflictDoUpdate(...)`
  - Transaction-like ops: sequential inserts/deletes for archive moves
  - Type inference from existing types: `KanbanStory` from ~/types/kanban.ts

---

## 2026-01-26 - Phase 01: SQLite Database (Task 3/9)

- Created app/db/index.server.ts - DB singleton with WAL mode
- Added ssr.external for better-sqlite3 in vite.config.ts
- Files changed: app/db/index.server.ts, vite.config.ts, CLAUDE.md
- **Learnings:**
  - WAL mode: `sqliteDb.pragma("journal_mode = WAL")`
  - Vite SSR needs `ssr: { external: ["better-sqlite3"] }` to avoid bundling native modules
  - better-sqlite3 uses sync API, no promises needed
  - DB location: ~/.claude/cc-viz/kanban.db

---

## 2026-01-26 - Phase 01: SQLite Database (Task 2/9)

- Created app/db/schema.ts with Drizzle schema
- Tables: stories, sessions, stories_archive, sessions_archive, metadata
- Files changed: app/db/schema.ts, CLAUDE.md
- **Learnings:**
  - Drizzle uses `sqliteTable` from `drizzle-orm/sqlite-core`
  - FK cascade deletes via `references(() => table.id, { onDelete: "cascade" })`
  - Archive tables mirror active tables with extra `archivedAt` field
  - Type exports: `$inferSelect` and `$inferInsert` for type inference

---

## 2026-01-26 - Phase 01: SQLite Database (Task 1/9)

- Added SQLite dependencies: better-sqlite3, drizzle-orm, drizzle-kit, @types/better-sqlite3
- Files changed: package.json, pnpm-lock.yaml
- **Learnings:**
  - pnpm warns about build scripts being ignored for native modules - `pnpm approve-builds` if needed
  - Drizzle ORM v0.45.1 is current

---

