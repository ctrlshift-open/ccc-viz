## Codebase Patterns

- Package manager: pnpm
- Test framework: vitest
- React Router 7 with SSR - dynamic imports required for Node.js modules in loaders

---

## 2026-01-25 - ccc-viz-o1b7 (Phase 01)

- Created `app/types/kanban.ts` with kanban board types
- Files changed: `app/types/kanban.ts`
- **Learnings:**
  - Types include: KanbanStatus, KanbanCard, KanbanState, CreateCardInput, UpdateCardInput, MergeCardsInput
  - Column order: archive, back-log, in-progress, discard, complete
  - Cards track sessionIds array for merge support

---

## 2026-01-25 - ccc-viz-o1b7 (Phase 01 - Server Utils)

- Created `app/utils/kanban.server.ts` with server-side state management
- Files changed: `app/utils/kanban.server.ts`, `package.json` (added nanoid)
- **Learnings:**
  - State stored at `~/.claude/cc-viz/kanban.json`
  - Uses nanoid for generating card IDs
  - `syncSessionsToCards()` handles initial import (→ archive) vs subsequent imports (→ in-progress)
  - `generateTitle()` extracts title from git branch or last message
  - Reuses `getSessionPreview()` and `getProjects()` from existing server utils
  - Standard Node.js imports (fs, path, os) ok at top-level for .server.ts files

---

