## Codebase Patterns

- Package manager: pnpm
- Test framework: vitest
- React Router 7 with SSR - dynamic imports required for Node.js modules in loaders
- CSS animations: custom keyframes in app.css, use `animate-` prefix for animation classes
- Panel pattern: fixed position + backdrop + body scroll lock + escape key handler
- Selection state pattern: useState for ID, useMemo to derive full object from state
- Prop threading: parent passes selection state + callback, child receives isSelected + onSelect
- Click vs drag detection: track mousedown position with useRef, compare on click (5px threshold)

---

## 2026-01-26 - Phase 04 (ccc-viz-8r2o)

- Added card selection with click handler and visual ring
- Implemented click vs drag differentiation using mousedown position tracking
- Removed inline editing from StoryCard (editing now panel-only)
- Files changed: StoryCard.tsx, KanbanColumn.tsx, KanbanBoard.tsx
- **Learnings:**
  - Use useRef to track mousedown position, compare on click event for drag detection
  - 5px movement threshold is good balance between precision and tolerance
  - Removing features simplifies code significantly (went from 283 lines to 203 lines in StoryCard)

---

## 2026-01-26 - Phase 03 (ccc-viz-w931)

- Threaded selectedStoryId and onStorySelect props through KanbanColumn to StoryCard
- Files changed: KanbanColumn.tsx, KanbanBoard.tsx, StoryCard.tsx (props only)
- **Learnings:**
  - Keep prop names consistent: parent uses IDs, child uses booleans (isSelected vs selectedStoryId)
  - Callback naming: parent says "select", receives ID; child calls with its own ID

---

## 2026-01-26 - Phase 02 (ccc-viz-w651)

- Added selection state and panel rendering to KanbanBoard
- Files changed: app/components/KanbanBoard.tsx
- **Learnings:**
  - Keep state minimal (ID only), derive full object via useMemo
  - Props for KanbanColumn deferred to Phase 03

---

## 2026-01-26 - Phase 01 (ccc-viz-yob3)

- Created StoryDetailPanel.tsx slide-over component
- Files changed: app/components/StoryDetailPanel.tsx (new), app/app.css (animation)
- **Learnings:**
  - TailwindCSS v4 uses @theme instead of @layer base for theme customization
  - Custom keyframes go in @layer utilities or after base styles
  - Body scroll lock via document.body.style.overflow = "hidden"

---

